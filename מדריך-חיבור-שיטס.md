# 💚 הקבוצות שלנו — מדריך חיבור גוגל שיטס (2 דקות)

אתה מוסיף שורה בשיטס → זה מופיע באתר אוטומטית. ככה עושים את זה פעם אחת:

## 1. צור גוגל שיטס חדש
1. כנס ל- https://sheets.google.com → **גיליון חדש**
2. בשורה הראשונה (כותרות) כתוב בדיוק:
   | A | B | C | D | E | F |
   |---|---|---|---|---|---|
   | `name` | `desc` | `link` | `category` | `image` | `active` |
3. דוגמה לשורה 2:
   - **name:** `שכונה מדברים הכל`
   - **desc:** `כל העדכונים של השכונה`
   - **link:** `https://chat.whatsapp.com/XXXX` (קישור הזמנה אמיתי מהוואטסאפ!)
   - **category:** `שכונה`
   - **image:** קישור ישיר לתמונה (`https://...jpg`) — אם ריק, תופיע אות ראשונה מעוצבת
   - **active:** `כן` (כתוב `לא` כדי להסתיר זמנית בלי למחוק)

> מאיפה מביאים קישור לתמונה? מעלים תמונה לאתר חינמי (למשל postimages.org / imgbb) ומעתיקים את ה-**Direct link**. אפשר גם קישור שיתוף מגוגל דרייב.

## 2. שתף לצפייה
1. למעלה: **שתף → כל מי שיש לו קישור → צופה**
2. העתק את הקישור, למשל:
   `https://docs.google.com/spreadsheets/d/ABC123XYZ/edit`
3. ה-ID הוא מה שבין `/d/` ל-`/edit` → `ABC123XYZ`

## 3. חבר לאתר
1. פתח את הקובץ `config.js`
2. הדבק:
   ```js
   SHEET_ID: "ABC123XYZ",
   ```
3. שמור + רענן את האתר. זהו! 🎉

## 4. הרצה מקומית / העלאה לאינטרנט
- **מקומית:** `node server.js` → פתח `http://localhost:3000`
- **בלי שרת בכלל:** פשוט פתח `index.html` בדפדפן — האתר קורא את השיטס ישירות!
- **חינם לאינטרנט:** העלה את 4 הקבצים (`index.html, style.css, app.js, config.js`) ל:
  Netlify Drop / Vercel / GitHub Pages / Cloudflare Pages — הכל עובד, בלי שרת.

## טיפים 💡
- **קישור וואטסאפ:** בקבוצה → שם הקבוצה → `הזמן באמצעות קישור` → העתק.
- **קטגוריות:** נוצרות אוטומטית מעמודת `category`.
- **סדר:** הסדר בשיטס = הסדר באתר. גרור שורות כדי לשנות.
- **רענון:** האתר מתרענן לבד כל דקה + כפתור ↻.
- רוצה שאוסיף טופס "הוסף קבוצה" שכותב לשיטס? צריך Apps Script קטן — תגיד לי ואוסיף.
